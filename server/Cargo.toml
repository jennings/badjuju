[package]
name = "badjuju"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1.46.1", features = ["io-std", "macros", "rt-multi-thread"] }
tower-lsp = "0.20.0"
watchman_client = { version = "0.9.0" }
