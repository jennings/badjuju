;;; badjuju-transient.el --- Transient menus for Bad Juju  -*- lexical-binding: t; -*-

;;; Commentary:

;; Transient menus using the built-in `transient' library (same library
;; Magit uses).  Provides:
;;   ? — contextual help popup listing all Bad Juju keybindings
;;   c — commit popup (describe/reword, new, squash, …)
;; Full implementation lands in issue #45.

;;; Code:

(provide 'badjuju-transient)
;;; badjuju-transient.el ends here
