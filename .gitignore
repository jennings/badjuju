target/
.redo/
node_modules/
clients/vscode/out/
clients/vscode/dist/
clients/neovim/.test-deps/
/configuration
*.vsix

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Per-developer Claude Code overrides — settings.json is shared, .local is not.
.claude/settings.local.json
